import firebase from 'firebase/app';
import 'firebase/firestore';
import 'firebase/auth';
var config = {
    // Add Firebase Config over here
};

firebase.initializeApp(config);

export default firebase 